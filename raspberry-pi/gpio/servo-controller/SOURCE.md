# Source / Attribution

This folder is a curated example project in `rust-embedded-library`.

It uses the `rppal` crate (dependency) for GPIO access:

- Name: rppal
- Upstream: https://github.com/golemparts/rppal
- License: See upstream
- Changes to upstream code: None (dependency only)
